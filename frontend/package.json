{
  "dependencies": {
    "@reduxjs/toolkit": "^2.12.0",
    "firebase": "^12.13.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-redux": "^9.3.0",
    "react-router-dom": "^6.30.3"
  },
  "name": "namaste_r",
  "version": "1.0.0",
  "description": "> dev build(development build --for faster compliation,HMR soon..)\r > local server creates(localhost:1234)\r > HMR hot module replacement(HMR updates modules in the browser without refreshing the entire page.)\r > file watching algorithm -- written in cpp(monitos files metadata)\r > caching -- faster builds(.parcel-cache stores cached build data created by Parcel.)\r > image optimization--(Image optimization reduces the file size of images without noticeably reducing quality.)\r > minification--(Minification removes unnecessary characters from code without changing functionality.)\r > bundling\r > compressing\r > consistent hashing--(When content changes, the hash changes automatically, forcing the browser to download the latest file.)\r > code spliting\r > differential bundling -- support old browers\r > diagnostic\r > error handling\r > https\r > tree shaking -- remove unused code(Tree Shaking removes unused code from the final bundle.)\r > different dev and production bundles--\r > (--prod biuld takes time compares to dev\r > --optimization is more than dev)",
  "devDependencies": {
    "parcel": "^2.16.4",
    "postcss": "^8.5.14",
    "rimraf": "^5.0.10",
    "tailwindcss": "^3.4.19"
  },
  "scripts": {
    "start": "scripts\\start.cmd",
    "start:frontend": "parcel index.html --port 3000",
    "start:clean": "rimraf .parcel-cache dist .tmp && parcel index.html --port 3000",
    "build": "parcel build index.html",
    "build:clean": "rimraf .parcel-cache dist .tmp && parcel build index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
